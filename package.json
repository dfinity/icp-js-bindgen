{
  "name": "@icp-sdk/bindgen",
  "version": "0.0.1",
  "description": "Generate JavaScript bindings for IC canisters",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ./src/core/generate/rs --target nodejs --out-name icp-js-bindgen --out-dir ./dist --dev --no-pack",
    "build": "pnpm wasm && pnpm vite build",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "@tanstack/config": "^0.20.1",
    "@types/node": "^24.3.1",
    "vite": "^7.1.5",
    "vite-plugin-static-copy": "^3.1.2",
    "vitest": "^3.2.4"
  }
}
