// A candid type whose name equals the class name derived from the .did basename
// (`collide_class` -> `Collide_class`). Both are declared into the same module, so the
// generated file declares one name twice.
type Collide_class = record { a : nat };

service : () -> {
  test : (Collide_class) -> ();
}
