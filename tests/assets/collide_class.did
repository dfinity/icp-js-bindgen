// A candid type whose name equals the class name derived from the .did basename
// (`collide_class` -> `Collide_class`). Both are emitted into the same module.
type Collide_class = record { a : nat };

service : () -> {
  test : (Collide_class) -> ();
}
